import HomePage from "./pages/HomePage";

// Only the home page is wired up for now, per request.
// Other pages listed in /src/pages (About, Products, FAQ, Blog, Contact, product
// detail pages) can be added to a router (e.g. react-router-dom) later.
function App() {
  return <HomePage />;
}

export default App;
